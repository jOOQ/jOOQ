sbtPlugin := true

version := "${project.version}"

organization := "${project.groupId}"

name := "${project.artifactId}"

// latest 2.9.x, 2.10.x and 2.11.x releases as of 2014/09/01, modify as needed
crossScalaVersions := Seq("2.9.3", "2.10.4", "2.11.2")

